import { RecordDigitizationWorkspace } from "@/features/record-digitization/components/RecordDigitizationWorkspace";

export default function RecordDigitizationPage() {
  return <RecordDigitizationWorkspace />;
}
